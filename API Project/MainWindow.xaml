<Window x:Class="API_Project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:API_Project"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Eingabe- und Suchbereich -->
            <RowDefinition Height="*" />
            <!-- Suchergebnisse -->
        </Grid.RowDefinitions>

        <!-- Eingabebereich -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Grid.Column="0" Margin="0,0,10,0" 
                 VerticalAlignment="Center" FontSize="14" />
            <Button x:Name="SearchButton" Grid.Column="1" 
                Content="Suchen" Width="80" 
                VerticalAlignment="Center"
                Click="SearchButton_Click" />
        </Grid>

        <!-- Suchergebnisse -->
        <TextBox x:Name="SearchResults" Grid.Row="1" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True"/>
    </Grid>
</Window>
